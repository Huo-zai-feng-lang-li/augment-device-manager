{
  "name": "cursor-augment-solution",
  "version": "1.0.0",
  "description": "Cursor Augment设备限制解决方案",
  "main": "index.js",
  "scripts": {
    "dev": "echo 🚀 启动完整开发环境... && npm run install-all && concurrently --names \"后端,客户端\" --prefix-colors \"blue,green\" \"cd admin-backend && npm run dev\" \"cd desktop-client && npm run dev\"",
    "build": "echo 🚀 智能打包远程控制版本... && node smart-build.js",
    "start": "npm run dev",
    "server": "cd admin-backend && npm run dev",
    "client": "cd desktop-client && npm run dev",
    "release": "cd desktop-client && npm run release",
    "install-all": "npm install && cd admin-backend && npm install && cd ../desktop-client && npm install",
    "server-only": "echo 🌐 启动远程控制服务器... && cd admin-backend && npm run dev",
    "setup": "echo 📦 安装所有依赖... && npm run install-all && echo ✅ 安装完成！运行 npm run dev 启动开发环境"
  },
  "keywords": [
    "cursor",
    "augment",
    "device-limit",
    "activation"
  ],
  "author": "Developer",
  "license": "MIT",
  "devDependencies": {
    "axios": "^1.10.0",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "fs-extra": "^11.3.0"
  }
}
